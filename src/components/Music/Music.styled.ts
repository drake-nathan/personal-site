import styled from '@emotion/styled';

export const StyledMusicContainer = styled.div`
  width: 80vw;
  margin-left: 10vw;

  display: grid;
  gap: 2rem;
  grid-template-columns: repeat(2, 1fr);
`;

export const StyledMusicText = styled.p`
  width: 60ch;
  text-align: left;
`;
