import styled from '@emotion/styled';

export const NavContainer = styled.div`
  font-size: 1.25rem;
  font-weight: 400;
  color: var(--color-text-offset);
  margin-top: 2.5em;

  .link {
    margin: auto 20px;
    padding-bottom: 3px;
  }
`;
